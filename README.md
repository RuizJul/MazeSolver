# MazeSolver
Java-based maze-solving game that uses tree data structures and the Minimax algorithm to model decision-making. The maze is represented as a state tree where each node is a possible position, allowing an intelligent agent to evaluate paths and choose the optimal route to escape.
